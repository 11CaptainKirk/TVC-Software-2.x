
void stateMachine(int systemState)
{
    switch (systemState)
    {
    case 0:
        // Initializing ( Not Ready )
        break;
    case 1:
        // Ground Idle ( Ready For Flight )
        break;
    case 2:
        // Ground Idle ( Ready For Flight )
        break;
    default:
        // code block
    }
}